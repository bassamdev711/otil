# مِفتاح — رابط الشبكة المحلية

## الفكرة

يحتفظ هاتف Android بقاعدة البيانات الوحيدة، ويخدم تطبيق الهاتف واجهة Web وواجهة البيانات معاً. لا يحتاج اللابتوب إلى تنزيل نسخة Web أو تشغيل خادم محلي.

## التشغيل

1. ثبّت `Miftah-Android-LAN-ARM64-v1.0.0+2.apk` على هاتف Android حديث ARM64.
2. افتح التطبيق وسجّل الدخول. يجب إبقاء تطبيق مِفتاح مفتوحاً أثناء استخدام المتصفح.
3. تأكد من اتصال الهاتف واللابتوب بشبكة Wi‑Fi نفسها، أو فعّل نقطة اتصال الهاتف ووصل اللابتوب بها.
4. من إعدادات المدير داخل تطبيق الهاتف، انسخ **رابط الشبكة المحلية** الظاهر، وسيكون شبيهاً بـ:

```text
http://192.168.1.25:8787
```

5. افتح هذا الرابط فقط في متصفح اللابتوب. سيحوّل الهاتف المتصفح تلقائياً إلى جلسة آمنة برمز مؤقت ويعرض الواجهة.

لا تفك ضغط أي ملف Web، ولا تشغّل `python3 -m http.server` على اللابتوب. كل البيانات تبقى في الهاتف، وكل قراءة وكتابة من المتصفح تمر إلى قاعدة الهاتف نفسها.

## ملاحظات التشغيل

يجب ألا يغلق المستخدم تطبيق الهاتف أثناء اتصال المتصفح، ويجب تعطيل تقييد البطارية لتطبيق مِفتاح. إذا تغيرت شبكة Wi‑Fi أو تغير عنوان الهاتف، انسخ الرابط الجديد من إعدادات الهاتف. المتصفح يحدّث حالته من الهاتف دورياً، وأي تعديل يُحفظ مباشرة في قاعدة الهاتف.

ملف Web المضغوط القديم ليس مطلوباً لهذا الوضع، ويمكن تجاهله. ملف AAB مخصص لـ Google Play ولا يُثبّت مباشرة. ملف `Miftah-LAN-checksums.txt` يحتوي على بصمات SHA-256 للحزم.
